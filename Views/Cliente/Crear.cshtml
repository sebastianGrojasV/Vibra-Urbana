@model VibraUrbana.ViewModels.CrearClienteViewModel

@{
    ViewData["Title"] = "Registrar Cliente";
}

<section class="vu-admin-shell">
    <div class="container-fluid">
        <div class="row g-0">
            @await Html.PartialAsync("_Sidebar")

            <div class="col-lg-9 col-xl-10 vu-admin-content">
                <div class="vu-admin-header">
                    <div>
                        <p class="vu-eyebrow">Gestión comercial</p>
                        <h1>Registrar cliente</h1>
                        <p>Agrega clientes activos para utilizarlos después en ventas, pedidos y facturación.</p>
                    </div>
                    <a class="btn btn-outline-primary" asp-controller="Cliente" asp-action="Index">Volver al listado</a>
                </div>

                <div class="row g-4">
                    <div class="col-xl-8">
                        <div class="vu-panel">
                            <form asp-controller="Cliente" asp-action="Crear" method="post" novalidate>
                                @Html.AntiForgeryToken()
                                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label asp-for="Identificacion" class="form-label"></label>
                                        <input asp-for="Identificacion" class="form-control" autocomplete="off" />
                                        <span asp-validation-for="Identificacion" class="text-danger small"></span>
                                    </div>
                                    <div class="col-md-6">
                                        <label asp-for="NombreCompleto" class="form-label"></label>
                                        <input asp-for="NombreCompleto" class="form-control" autocomplete="name" />
                                        <span asp-validation-for="NombreCompleto" class="text-danger small"></span>
                                    </div>
                                    <div class="col-md-6">
                                        <label asp-for="Telefono" class="form-label"></label>
                                        <input asp-for="Telefono" class="form-control" autocomplete="tel" />
                                        <span asp-validation-for="Telefono" class="text-danger small"></span>
                                    </div>
                                    <div class="col-md-6">
                                        <label asp-for="Correo" class="form-label"></label>
                                        <input asp-for="Correo" class="form-control" autocomplete="email" />
                                        <span asp-validation-for="Correo" class="text-danger small"></span>
                                    </div>
                                    <div class="col-12">
                                        <label asp-for="Direccion" class="form-label"></label>
                                        <textarea asp-for="Direccion" class="form-control" rows="3"></textarea>
                                        <span asp-validation-for="Direccion" class="text-danger small"></span>
                                    </div>
                                </div>

                                <div class="d-flex flex-wrap gap-2 mt-4">
                                    <button class="btn btn-primary" type="submit">Guardar</button>
                                    <a class="btn btn-outline-primary" asp-controller="Cliente" asp-action="Crear">Limpiar</a>
                                </div>
                            </form>
                        </div>
                    </div>

                    <div class="col-xl-4">
                        <div class="vu-panel">
                            <h2>Datos del cliente</h2>
                            <ul class="vu-status-list">
                                <li><span></span> La identificación debe ser única</li>
                                <li><span></span> El cliente queda activo por defecto</li>
                                <li><span></span> Los datos se usarán en ventas y pedidos</li>
                                <li><span></span> Puedes desactivarlo sin borrar su historial</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

