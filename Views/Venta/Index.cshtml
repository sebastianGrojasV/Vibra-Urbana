@{
    ViewData["Title"] = "Ventas";
}

<section class="vu-module-placeholder-page">
    <div class="container">
        <div class="vu-module-placeholder">
            <span class="vu-module-placeholder-icon" data-vu-module-icon="ventas"></span>
            <p class="vu-eyebrow">Módulo en preparación</p>
            <h1>Ventas</h1>
            <p>En el siguiente sprint se integrará el flujo comercial para registrar ventas y sus detalles.</p>
            <div class="vu-placeholder-actions">
                <a class="btn btn-primary" asp-controller="Admin" asp-action="Index">Volver al panel</a>
                <a class="btn btn-outline-primary" asp-controller="Factura" asp-action="Index">Ver facturación</a>
            </div>
        </div>
    </div>
</section>
